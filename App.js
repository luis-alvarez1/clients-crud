import React from 'react';
import {
  SafeAreaView,
  StyleSheet,
  ScrollView,
  View,
  Text,
  StatusBar,
} from 'react-native';
import Navigator from './src/navigation/Navigator';

const App = () => {
  return (
    <>
      <Navigator />
    </>
  );
};

const styles = StyleSheet.create({});

export default App;
